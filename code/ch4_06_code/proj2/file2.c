#include <stdio.h>
#include "file1.h"

int main(void)
{
    printstr();
    printstr();
    printstr();
    printstr();
    printstr();
//    printf("%d, %d.\n", i, j);  //去掉直接调用静态局部变量的语句。

    return 0;
}
